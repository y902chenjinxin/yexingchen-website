# 项目版本信息

## 当前版本
- **版本号**: v1.5.1
- **版本名称**: 神农图·架构优化
- **发布日期**: 2026-05-28

## Git 仓库
- **地址**: https://github.com/y902chenjinxin/yexingchen-website
- **分支**: master (主分支)

## 版本迭代记录

### v1.5.1 (2026-05-28) ✓ 架构优化
- 抽取通用 CRUD Store（`useCrudStore.js`），music/novel/video/tool 四个 store 从 ~37 行简化为 8 行
- JWT payload 写入 `is_super_admin`，后端 `require_super_admin` 优先读 token，减少数据库查询
- AdminView 用户表格添加空状态占位（无用户时显示"暂无用户"）
- 新增用户添加邮箱格式校验（正则 `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`）

### v1.5.0 (2026-05-28) ✓ 交互体验升级
- 输入框 placeholder 颜色从灰色改为淡蓝灰色 #8B9DAF
- 登录错误提示改为"账密输入错误，请重试"
- 登录密码显隐图标（默认闭眼 SVG，点击切换）
- 登录加载仙气飘飘特效（浅紫淡蓝渐变+纱幔+细线环+萤火虫粒子）
- 背景音乐名称更新（青花瓷、兰亭序）
- 岛屿新增阵法模式（右上角✧按钮切换环形旋转叠加动画）
- 5个岛屿从卡片改为数据列表（名称/线上地址/标签/大小/操作）
- 脚本密码更新：yxCHEN@12345678
- 页面加载缩短至 0.8s（动画速度加快）
- 登录门从旋转改为水平滑开（两边对称分开）
- 删除门展开后的抖动效果
- 登录卡片延迟增加 0.6s
- 背景音乐 MP3 修复播放
- 岛屿周转动画（5岛屿围成圈自动轨道运动）
- 输入框改为白底黑字
- 管理员入口可见（登录后显示管理后台）

### v1.3.1 (2026-05-28) ✓ 登录功能修复
- 登录门动画：从 rotateY 旋转改为 translateX 水平滑开（两边分开）
- 登录过期误报修复：fetchUser 失败时只清本地状态，不调用 logoutAPI
- 401 响应拦截器：只做路由跳转和清除 token，不显示错误消息
- 数据库密码 hash 重新生成并更新（服务器 Python venv 环境）
- 服务器 root 密码更新为 yxCHEN@12345678
- 部署流程：确保所有组件 CSS 文件（LoginView-*.css）都上传

### v1.3.0 (2026-05-28) ✓ 岛屿周转动画
- 岛屿动画从上下浮动改为周转轨道运动
- 音乐岛：12s周期，±8px, ±12px, ±2°
- 小说岛：15s周期，±10px, ±15px, ±2.5°
- 视频岛：18s周期，±6px, ±10px, ±1.5°
- 日志岛：14s周期，±12px, ±14px, ±3°
- 工具岛：16s周期，±9px, ±13px, ±2°
- 背景音乐格式从 wav 改为 mp3
- 部署流程规范化：先本地preview验证，再清理旧文件后完整上传

### v1.2.0 (2026-05-27) ✓ 仙侠风格
- 登录页重设计：水墨背景 + 青绿灵气光效 + 磨砂玻璃卡片 + 古朴金边框
- 加载动画延长至 1.5s
- 五岛屿重设计：仙侠浮空岛风格（夜空+月+远山+云雾+灵气光点）
- 右上角头像下拉菜单：个人中心/密码修改/头像选择/岛屿导航
- 去除底部岛屿导航按钮
- 备案信息展示：公安备案+网络备案图标+链接
- 背景音乐生效：garden_music.wav 已上传并配置
- RBAC：用户表增加 is_super_admin、is_test_user 字段
- 测试数据管理：各内容表增加 is_test_data 字段
- 后端权限校验支持 is_super_admin=1

### v1.1.0 (2026-05-27) ✓ 岛屿SVG已确认
- 登录页视觉重设计：年轻化渐变紫蓝配色
- 光效优化：纯净白光扫过效果
- 开门效果增强：增加光芒绽放动效
- 主页面背景改为渐变蓝天
- 五岛屿独立SVG设计图（音乐/小说/视频/日志/工具）✓
- 新增测试数据生成脚本 `generate_test_data.py`
- 背景音乐功能 `generate_bg_music.py`
- 代码优化：删除废弃的 `.island-icon` 样式，岛屿导航弹层图标统一为SVG，音乐按钮去除冗余 `!important`

### v1.0.0 (2026-05-26)
- 初始版本发布
- 神农图主题，云岛风格
- 5个内容岛屿：音乐、小说、视频、日志、工具
- FastAPI + Vue3 + SQLite 后端架构
- 邮箱注册+管理员审批流程
- JWT认证
- Nginx反向代理配置
- 腾讯云COS集成

## 部署路径
- **后端**: /var/www/yexingchen/backend
- **前端**: /var/www/yexingchen/dist
- **日志**: /tmp/yexingchen_new.log