# 玄黄 · 当前状态（断档接续入口）

> 后续智能体/协作者接续工作的第一份文档。先读本文件了解「现在到哪了」，需要细节再翻 `WORK_LOG.md`（交付台账）与 `HISTORY.md`（演进与踩坑）。

## 一、项目定位

云上「玄黄」个人平台：浮空岛（音乐/小说/视频/日志/工具）+ 个人工作台（笔记/任务/资产/AI 助手/回收站/个人资料）。
技术栈：Vue3 + Vite + Pinia + Element Plus | FastAPI + SQLAlchemy + SQLite | Nginx + PM2 + PWA。
线上：https://yexingchen.cn（腾讯云 CVM 203.195.208.25，OpenCloudOS 9.4）。

## 二、最近交付（9/20 最新）

| 时间 | 内容 | Commit |
|------|------|--------|
| 2026-09-21 | **工作台 8 项功能优化 + 修复「工作台 ability 必填」（SW `xuanhuang-v146`，v2.38.0，User「1-8 都做了吧」「弄完把代码优化下、补状态、提交 GitHub」）**：① **修复工作台报错**——`AiBriefCard` 走自定义 `POST /api/workbench/dashboard/brief`（后端组装数据调 AI 生成简报，返回 summary），不再直接调 `ai.preview` 避免缺 `ability` 422。② **#1 习惯打卡 + Streak**：`xuanhuang_habits`/`xuanhuang_habit_checkins` 两表（迁移 `s0t1u2v3w4x5` 链到 head，前端/迁移测试同步）+ `routers/workbench/habits.py`（CRUD/打卡 toggle/连续天数/本周次数/30 天热力图）+ `HabitsCard.vue`。③ **#2 实时天气小部件**：`WeatherCard.vue` 纯前端走 Open-Meteo（含 IP 定位回退默认南京 + 手动城市选择 + localStorage 缓存）。④ **#3 桌面日历联动倒计时 badge**：`GlobalTopBar` 新增胶囊按钮显示最近未过期倒计时 + 展开面板、点按跳详情。⑤ **#4 资讯流已读/未读统计 + 智能滚动**：`FeedsView` 未读角标 + 「未读」筛选 + 翻页自动滚动到首条未读并高亮。⑥ **#5 空状态美化**：新增 `EmptyState.vue`，应用到 TasksView/SubscriptionsView/QuickView/MusicView/VideoView/ToolView。⑦ **#6 键盘速记**：任意页非输入态按 `N` 直跳新建笔记。⑧ **#7 快捷面板模块显隐**：`ProfileView` 增习惯/天气/资讯/AI 简报开关，`prefs` store `moduleVisible` 持久化，工作台按开关渲染。⑨ **#8 暗色 OLED 真黑**：`desktop-theme-night.css` 夜间背景退纯黑、表面保留极浅层次。**验证**：前端 `vite build` 通过、后端 `pytest` 3 例迁移测试通过 | 本轮 |
| 2026-09-20 | **修复：手机端音乐列表空白 + AI 语音两处（SW `xuanhuang-v143`，v2.36.0，User「1 手机端不展示音乐列表；2 手机端AI对话音频无法使用；3 电脑端AI对话音频输入时没有在输入框停留，直接发送了」）**：① **音乐列表空白＝`mobile-deink.css` 把 `.manage-pane`（即曲库列表本身）整块 `display:none`**——旧架构音乐页内嵌迷你播放器才「进去即播放器」、隐藏曲库合理；现播放器已移到**全局底栏**，`MusicView` 的 `.manage-pane` 就是唯一内容，被该规则隐藏 → 手机端 `/music` 只剩空壳。改为 `#app.is-mobile .island-inner:not(.island-inner-music) .manage-pane{display:none}`（音乐岛不命中 `:not()` 故不再隐藏，视频/小说/工具仍按旧策略收）+ 新增 `#app.is-mobile .island-inner-music .manage-pane{display:block}` 玻璃卡片化 + 收掉「搜索/批量删除/分页」；`MusicView.vue` 加 `useIsMobile()`，手机端绕过每页 10 条切片做全量展示（否则隐藏分页只能看前 10 首）。② **手机端 AI 语音不可用**：`VoiceInputButton` 在 `supported=false`（APK WebView / iOS Safari 无 Web Speech API）时 `v-if` 整按钮静默消失、无任何提示 → 改为**始终渲染**麦克风按钮，不支持时半透明 + `cursor:not-allowed` + 点击弹 `ElMessage.warning('当前设备或浏览器不支持语音输入，请直接打字')`。③ **桌面端语音自动发送**：`AssistantView.onVoiceDraft` 写草稿后立即 `send()` → 改为**只填输入框**（多段换行拼接）+ `nextTick` 后 `focus()` 到 `el-input`（新增 `composerInputRef`），由用户手动发送；与全站其它语音输入「只填不自动发」统一，桌面/手机一致。**桌面回归**：MusicView 桌面 `isMobile` 恒 false、分页逻辑不变。**部署/验证**：SW `v143`、全新 `vite build --outDir dist2` 核验标记 → 替换 dist → `deploy_frontend.py` 226 文件、服务器 sw.js `xuanhuang-v143`、home=200；仅前端后端不动。真实浏览器取证 PASS（/music `.manage-pane` `display:block/visible` 含 5 首歌、/assistant `.vib` 可见）；移动端 is-mobile 分支按 CSS 特异性严格成立。详见 `WORK_LOG.md` | 本轮 |
| 2026-09-19 | **三问题最终闭环（SW `xuanhuang-v128`，v2.35.0，User 追问证件照按钮实际走的抠图路径）——全部以真实浏览器端到端取证（登录→页面操作→截图+网络请求+数据库三方对账）**：① **证件照前端解包修复（"API 通了但页面仍抠不出人"的真根因）**：axios 拦截器已 `return response.data`，`matteFromBackend` 又 `const { data } = await api.post()` **二次解构恒 undefined** → 静默回退旧 flood-fill；改 `const body = await api.post()` 取 `body.data`，页面实际走后端 AI matte，人物完整抠出。② **倒计时背景图 URL 缺 `/uploads` 前缀**：`save_upload_file` 返 `/countdown/xxx.jpg` 而静态挂载在 `/uploads`（nginx alias），URL 被 SPA fallback 兜底成 HTML 假图；`countdown.py` 上传返补前缀（对齐 travels.py）+删除时剥前缀兼容；取证 DB→URL 返 image/jpeg→卡片渲染星空图→删卡自动清文件。③ **足迹标记微调**：`FOOT_SCALE 0.10`+白描边光环，实测 1/20~1/30 省份、清晰不喧宾。线上测试残留全清（2 测试卡片页面删除、假 HTML 文件与孤儿文件已清、uploads/countdown 归零），本地临时素材已删，v2.34.0+v2.35.0 一并提交推送 | 本轮 |
| 2026-09-19 | **三问题现网复验修复（SW `xuanhuang-v127`，v2.34.0·最终落地，User「证件照还是扣不出人、线上测试也有问题；足迹图标不好看且偏大；倒计时你到底试过没，自己上现网传下」）——本次全部用真实浏览器在生产逐项实测 + 线上 API 取证**：① **证件照 AI 抠图——迁到后端 CPU 推理（根治）**：浏览器 wasm 对动态 INT8 量化 MODNet 输出**全零**（人物被判为背景→被整体抠没）；最终**删除前端 wasm 推理，新增后端 `routers/idphoto.py` `/api/idphoto/matte`**（onnxruntime CPUExecutionProvider 跑 model-q.onnx，`requirements.txt`+onnxruntime，白名单补 idphoto+模型上传）。本地实测前景 44.45%；**线上 API 实测 PASS**：`/matte` 200、前景 **0.4448**、min/max 0/255，人物完整抠出。② **倒计时上传+保存**：根因 `api/index.js` 全局 `Content-Type: application/json` 使 axios 对 FormData 走 formDataToJSON 清空 file → 422；**移除全局 Content-Type** 交 axios 自动判定；`CountdownsView` 动态校验 `target_date` 仅非农历必填。线上实测 **upload 200+create 200**；**真实浏览器实测**：注入图片上传成功、无报错。③ **足迹地图标记**：琥珀定位针（teardrop pin+白色光晕环+底部投影），默认琥珀渐变/hover 转青辉光，`FOOT_SCALE 0.10`；全新浏览器实测约 **1/20~1/30 省份、10–14px**，小且不遮盖（此前"大青圆点"是旧 SW 缓存旧版，v127 已清缓存）。**部署**：前端 dist2 全新构建核验 SW v127 → 替换 dist → `deploy_frontend.py`(222 文件 home=200)；后端 `deploy_backend.py`(onnxruntime 安装/模型上传/health=200)；SW v126→v127 强刷缓存。已清线上测试残留（测试倒计时、故障期 0 字节暂存）与本地临时脚本/测试图；测试号密码重置为已知值并记录 | 本轮 |
| 2026-09-19 | **修复：网页端返回按钮「消失 + 失效」双问题（SW `xuanhuang-v117`，User「有些页面的返回按钮失效/消失了（是网页的，手机端我不需要返回按钮）」）**：**根因两处**——① **消失**：桌面端 `GlobalTopBar` 60px 固定顶栏(z-index:1000)悬浮，岛屿/工具模块共享外壳 `IslandInnerBase` 的 `.inner-header` 无顶栏避让(仅 26px) → 页首 `.back-btn` 落在顶栏下被盖住；`ProfileView` 顶部 40px 亦被盖。② **失效**：`goBack()` 桌面写死 `router.push('/workbench')`，忽略「回来源页」，从 /stocks 进详情点返回硬跳工作台而非回列表。**修法**：`IslandInnerBase.vue` `.inner-header` 桌面 padding-top → `84px`(1100px 断点 80px)、`goBack()` 统一「有历史 `history.back()`，无历史才跳工作台」，桌面/移动一致；`ProfileView.vue` 删重复文字版返回、顶部 padding 40→84px。构建 `vite build dist2` 核验 `84px 40px 14px` 进 CSS+`history.back` 进 JS+SW v116→v117 →替换 dist→`deploy_frontend.py` 220 文件 home=200。**生产桌面浏览器取证 PASS**（Playwright 1440×900 真 chromium）：/music /tool /finance /stocks /profile /travels /contacts /notes /assistant 九页返回按钮全部可见(rect 128×37 或 80×32, top≥84px 未被顶栏遮挡)、/profile 仅 1 个返回按钮；导航链 工作台→音乐→返回=工作台 ✓、/stocks→/stocks/600519→返回=**/stocks** ✓（旧逻辑错跳工作台）、/finance→返回=工作台 ✓。**手机端零回归**（390×844）：`.app-back-btn/.back-btn` 均 `display:none` 隐藏，符合「手机端不需返回按钮」。仅前端，后端无源码变更 | 本轮 |
| 2026-09-18 | **修复：模块内页白天模式仍深灰（全站岛屿内页背景昼夜化，SW `xuanhuang-v116`，User「外面我用白天的样式，进入各个模块里面依然是灰色的，其他模块也一样」）**：**根因**——所有岛屿/工具模块页共用 `IslandInnerBase.vue` 外壳，其 `.island-inner::before` 写死深色渐变 `linear-gradient(168deg,#1b262f 0%,#131b22 55%,#10161c 100%)`，无视 `data-theme="day"`；工作台/登录/岛屿主页(`.island-page`)早有日间覆盖，唯独这层没绑 token。**修法**：`variables.css` 增 `--li-sky/-mid/-deep/--li-title-a` 夜日双套 token（日间与 Linear 亮表 `#eef1f3` 同族）；`IslandInnerBase.vue` 背景渐变改 `--li-sky*`、大标题渐变顶改 `--li-title-a`（日深墨防白字埋底），桌面/移动同步。构建 `vite build dist2` 核验 `--li-sky` 夜/日两值 + SW v115→v116 →替换 dist→`deploy_frontend.py` 220 文件 home=200。**生产浏览器取证 PASS**：day 下 `--ls-bg1/--li-sky=#eef1f3`、`/tool/countdown` shell 背景 `rgb(244,247,248)` 浅色、截图浅灰白深字；night 仍深色。仅前端 | 本轮 |
| 2026-09-18 | **桌面端「深墨青玉 · Linear 产品风」收敛层（SW `xuanhuang-v115`，User「我不是要你把手机端复制到网页来」→ 桌面重做 Linear 式深色产品风，参考 Linear/shadcn dark/Supabase dark）**：新增 `desktop-product.css`（作用域 `#app:not(.is-mobile)`，手机端零回归）——**表面阶梯用边框亮度而非色块**（`--dp-bg #0b0f14 /--dp-bg2 #0f151b /--dp-surface #121820 /--dp-surface2 #18212b` + 发丝线 `--dp-line .10/--dp-line-strong .17`）；**Linear 式多层淡阴影** `--dp-shadow`（一层紧贴+一层远距柔化，尽量薄），hover 只聚焦描边不加位移；**单一青玉 `--dp-accent #4fc6b8` 克制点缀**，仅服于当前态/主按钮/焦点环/关键数；工作台卡片/资讯条/顶栏入口收口为实色面+发丝框（hover 聚光/active 压入/焦点描边）。main.js 于 `mobile-product.css` 后引入，SW `v110→v115` 清旧缓存。构建 `vite build dist2` 核验 `--dp-bg/--dp-line/--dp-accent: #4fc6b8` + SW v115 →替换 dist→`deploy_frontend.py`。**生产核验 PASS**：服务器 sw.js `xuanhuang-v115`、desktop token 进 `index-6eD2JQii.css`、home=200；桌面 Playwright 1440×900 深/日双主题截图验收卡片/表格收口、`#app.is-mobile` 门控零回归。仅前端 | bfa30bf |
| 2026-09-18 | **审 Codex 新手机 UI 去坑 + 产品化收敛（SW `xuanhuang-v110`，对 Codex 交付的 `mobile-product.css`「产品化收敛层」全量核对去坑）**：`mobile-product.css`（统一成数据工具、去掉模块渐变/玻璃碎片）与既有 `mobile-module-color.css`「灵动·独立渐变」层同载存在**选择器特异性冲突**（前者 `#app.is-mobile .fin-kpi-val`(1,2,0) 精度低于后者 `#app.is-mobile .fin-page .fin-kpi-val`(1,3,0)）→ 记账页**零值仍被渐变/语义色渲染**（本月支出 ¥0 呈青绿渐变、流水笔数 0 也带色）。**修 3 处硬坑**：① `countdown.js` BASE 被改 `/api/countdowns`→`/countdowns`（dev/prod 均无 rewrite，否则倒计时全端 404）改回；② `MobileWorkbenchHome` 声明 `count` 未赋值→「流水笔数」恒「—」，补 `d.month_count`；③ **删 `mobile-module-color.css`**（main.js 引用+文件本体，同步清 `mobile-native-glass.css` 失效注释）→ `mobile-product.css` 成唯一收敛层、根治零值异色；SW `v109→v110` 让已装 App/PWA 拿到修复。构建核验（新文案/`/api/countdowns`/`month_count` 在、`mobile-module-color` 不在）→替换 dist→217 文件、服务器 sw.js `xuanhuang-v110`、home=200。**生产浏览器取证 PASS**：记账页三零值计算色 `#f0f5f8` 中性正文、工作台零值不再带 `.up/.dn`、流水笔数「—」→「0」、A/B 双主题截图布局干净、桌面零回归。仅前端，后端无源码变更 | 本轮 |
| 2026-09-17 | **手机端 7 大模块「灵动卡片·独立渐变」重设计 + APK 深墨沉浸（SW `xuanhuang-v109`，User「音乐、笔记、足迹、记账、行情、通讯录、倒计时的 UI 风格、框架、排版需要重新设计下，现在好丑」→「灵动卡片+渐变」）**：新增 `mobile-module-color.css`（`#app.is-mobile` 门控）为 7 模块各定**独立高饱和渐变**——记账青绿 `#0fa07f→#2fd58a`/行情蓝紫 `#5b6ae0→#7f8dff`/足迹天蓝 `#2a9ece→#49c6e6`/通讯录珊瑚 `#f0643c→#ff8f6e`/笔记品蓝 `#7a5ce8→#a69bff`/倒计时琥珀 `#e08a3a→#f4b36a`/音乐桃粉 `#8b5cf6→#d94f9b`，逐模块接管 KPI 渐变条/渐变数值/渐变主按钮/图标块/倒计时玉简箔面；隐藏音乐古琴虚影/音符飘带古风残留；精简 `mobile-native-glass.css` 去全站统一蓝紫（避免与新层冲突，Element 弹层仍然蓝紫）。**APK 黄横栏根层修复**：`build_webview_apk.py` 主题 status/navigationBarColor/background 全设深墨 `#10140F`+浅色图标 → 重打 v2.0.0 部署。构建：清缓存 `vite build dist2` 核验 `mv-c1/fin-kpi-val`+SW v108→v109 →替换 dist→217 文件 home=200；390px 手机 A/B 双主题实测记账青绿/倒计时琥珀/etc 渐变大数+按钮生效、返回钮隐藏 PASS、桌面零回归。音乐本地 dev 白屏为 `/music` vite proxy 遮蔽（后端无页面路由），生产 SPA fallback 无碍。APK BUILD_CODE=0、同 keystore 签名可覆盖、`yexingchen-2.0.0.apk`+下载页 200。仅前端+APK，后端无源码变更 | 本轮 |
| 2026-09-17 | **手机端三处修复 + 接通丢失移动端CSS（SW `xuanhuang-v108`，User「头部底部还有黄色横栏/手机端不需单独返回按钮/个人中心去个人信息」）**：根因是 `mobile-ab-theme.css`、`mobile-deink.css` 从未在 `main.js` 引入，整套手机端改造没生效故黄条压不掉；本轮补上 import（`#app.is-mobile` 门控、桌面零影响）。三处：子页标题去金渐变→纯色无衬线；手机端子页返回按钮整体隐藏交系统手势；个人中心隐藏整张「基本信息」卡只留「界面偏好」。构建链修正：`npm run build -- --outDir` 转参失效产不完整 bundle，改 `npx vite build --outDir dist5`（1833 模块）核验标记后替换 dist、SW v107→v108，`deploy_frontend.py` 217 文件 home=200。390px 手机实测：音乐页顶/底黄条消失、返回钮隐藏、`/profile` 基本信息卡 `display:none,height=0`/偏好卡可见 PASS。桌面零回归：`useIsMobile`=767px 断点，≥768 时无 `is-mobile`、全站 142 处 `#app.is-mobile` 规则失效（自动化环境宽 hard-top 444px 无法 >768 实证，靠门控构造保证）。测试账号已清理 | 本轮 |
| 2026-09-17 | **手机端 P3 打磨 上线部署（SW `xuanhuang-v107`，User「继续，全部完成，然后部署、整理清理」）**：把「一个页面一个焦点」推到子页，纯 CSS（`#app.is-mobile` 门控、桌面零影响）+ 极少量模板包裹类（Music/Tool 顶栏）。**音乐 `/music`**：`.mu-admin-tools` 隐顶栏「管理/上传」+ `.manage-pane` 兜底 → 进页即**播放器主视图**，拿掉曲库管理/上传。**工具 `/tool`**：`.tl-admin-tools` 隐「管理/添加」→ 纯卡片浏览，`.tool-icon` 琥珀→蓝紫。**我的 `/profile`**：金渐变标题→无衬线纯色、古金卡片→玻璃`--ls-line`、隐角色行/「修改密码」/「←返回工作台」、主题分段金→蓝紫。部署 217 文件 home=200、**SW v106→v107**。本地浏览器 DOM 实测：音乐管理钮/我的角色/改密/返回全 `display:none`、播放器可见、标题去金无衬线、分段蓝紫 PASS；账本 `.fin-io` 防回归 `none` PASS。工具页真路由 `/tool` |
| 2026-09-17 | **手机端简化全案 P0骨架+P1/P2去古风 上线部署（SW `xuanhuang-v106`，User「继续做P1/P2清理古风装饰」→「继续，然后部署、收尾」）**：新增 `mobile-ab-theme.css`（`#app.is-mobile` 内重映射 core/lj/ls token 的 **A 白天液态玻璃 / B 夜间深空极简** 双套）+ `mobile-deink.css`（**P1** 隐藏 `.lj-paper-texture`、`--font-serif` 全局切现代无衬线；**P2** 账本隐藏 `.fin-io/.fin-filters/.fin-row-ops`+放大 KPI 大数，股票隐藏 `.st-td-edit` 与移动卡删除 `.st-card-h .st-btn.danger`+KPI 两列），全部门控 `#app.is-mobile`、桌面零影响；重建 `MobileWorkbenchHome`（几何Logo+今日速览+线性图标入口）+ 底部两 Tab 主页/我的。仅前端改动，后端无源码变更不重部署。部署 217 文件 home=200、**SW v106**（升版确保 SEO/SW 缓存刷新）；生产浏览器实测：账本 `.fin-io/.fin-filters/.fin-row-ops/.lj-paper-texture` 全 `display:none`、「记一笔」保留；合成 DOM 下股票删除按钮 `display:none`、KPI 两列 PASS | 本轮 |
| 2026-09-16 | **管理后台三需求落地 + 工作台视觉部署现网（SW `xuanhuang-v92`，User「1 用户密码重置 2 角色下拉+树状 3 菜单一/二级模块」「按方案 1 部署+验收」）**：① `AdminView` 用户编辑下拉新增「重置密码」+ 独立弹窗（`POST /admin/users/{id}/reset-password`）；② 用户编辑弹窗角色下拉改由角色表动态填充；③ 菜单管理改**树状**（一级+二级缩进+「一级/二级」标签+「上级」列），表单新增「上级模块」下拉，保存带 `parent_id`（后端 `Menu.parent_id` + `_validate_parent` 禁三级/禁自身 + 删含子的一级模块拦截，迁移 `j1k2l3m4n5o6` 生产已应用 `e7f8a9b0c1d2→j1k2l3m4n5o6`）。工作台视觉打磨（朱砂印章/山峦剪影/玻璃徽章/楼层载入）一并上线。部署：后端 ecosystem 重启 ENV=production 保留 health=200；前端 `npx vite build --outDir dist2`（`npm run` 转发参数失效产陈旧 bundle 的坑）→整体替换 dist→v92→195 文件 home=200。生产浏览器取证 PASS：工作台印章/山峦/徽章俱在，后台「重置密码」入口+弹窗、角色下拉、菜单树+上级下拉逐一可见 | 本轮 |
| 2026-09-15 | **桌宠极简（SW 跟随，User「这些选项我都不要，就进来默认展示、随机动作」「把手去掉、直接拖本体」）**：移除桌宠全部设置面板（动作方式/散步/隐藏），默认即展示、只做**随机动作**、不可隐藏、不切换模式；去掉右上抓手角标，改为**按住桌宠本体直接拖动**（轻点切换随机动作作互动反馈）；滚动自动让位保留。仅前端，`WhaleCompanion.vue` 净减 300+ 行 | 本轮 |
| 2026-09-15 | **全站备案标识昼夜自适应（User「找个能在白天样式下好好展示的颜色，全页面生效」）**：`SiteFooter` 由写死浅色的 `variant=dark` 改为**随 `html[data-theme]` 昼夜主题自适应的玻璃徽章**（自带底片 + backdrop），键用 `--ls-text/--ls-glass/--ls-line`——白天自动变浅底深字、夜间深底浅字，工作台/岛屿/全局页脚全站清晰；登录页备案文字透明度 0.5→0.88 不再依赖悬停。浏览器取证：day 主题 `--ls-text` 翻深/`--ls-glass` 翻亮符合预期 | 本轮 |
| 2026-09-15 | **股票每日研判改 AI（规则保底 + AI，User「研判是否调用AI？每日自动做调整」）**：原有「每日研判」本是 KlineChart 前端纯规则、未调 AI；本次全链路落实 AI 增强。后端 `stock_analysis.py` 调默认/启用 AI Provider（能力 `stock_analysis`）结合 K 线+规则基线+相关资讯产出 `level/summary/suggestion` 落库 `xuanhuang_stock_daily_analysis`（幂等覆盖，未配置降级规则）；`main.py` 仿 feed_sync 后台协程（仅生产）交易日 **15:35 后**每 5 分钟为「有自选股且当日未研判」用户自动生成+记持仓快照。接口 `GET/POST /api/stocks/analysis/{market}/{code}`（历史/立即生成）。详情页 AI 优先展示（`AI·盘后`+模型名），无记录回退盘中规则，新增「立即研判」按钮。迁移 `e7f8a9b0c1d2`（线性 head，生产已升级）。部署：后端 alembic `c3d4e5f6a7b8→e7f8a9b0c1d2`+pm2 health=200、ENV=production=1；前端 SW v88、195 文件 home=200；analysis 路由未鉴权 401。随带清理 `cos_service.py`+邮箱文案配置化 | ec1d45d |
| 2026-09-14 | **修复账本环形图「没展示全」（SW `xuanhuang-v82`，User「这个环还是没展示全」）**：取证环形图呈「3/4 圆 + 顶部 ~90° 对称缺口」，中心金额/7类图例正常、非布局挤压。根因 `DonutChart` 用 `stroke-dashoffset`（每段 `-off` 负值累加）切段绘制，负 offset 未正确归位致顶部留 1/4 圆周空白，上一轮删动画(把 opacity→1)未触及此缺口。修复：改**按累计圆心角 rotate 绘制**（每段 `transform=rotate(累计°*360-90)` + `dasharray=段弧长/C-段弧长`），彻底绕开 dashoffset，各段角度首尾相接成整圆；清理 `--seg` 无引用样式与重复 `totalLabel`。校验 FinanceView bundle 含 `frame/angle:y*360-90/rotate`、**无 `stroke-dashoffset`**；SW v81→v82、175 文件 home=200、线上 sw.js v82。仅前端 | 本轮 |
| 2026-09-14 | **账本页四项界面修复 + 桌宠抓手紧贴（SW `xuanhuang-v81`，User「结余有问题/占比没完全展示/鼠标滑动无数值无峰值/桌宠图标离按钮远」）**：① 桌宠抓手 `.whale-handle` 右上角→`top:50%;right:6px;translateY(-50%)` 紧贴鲸鱼右肩（生产实测 ≈-1px）；② 累计结余核实 `balance=Σincome−Σexpense`（软删过滤）逻辑正确、为负因历史支出>收入；③ 环形图图例新增 `fin-legend-pct` 占比列（7 分类全显不截断）+ `DonutChart` 中心 `toFixed(2)` 消除舍入误差；④ 趋势图 `TrendChart` 增鼠标悬浮（竖虚线+玻璃提示框：日期/收入/支出）+ 收支峰值圆点「峰 xxx」标注。仅前端 v81 175 文件 home=200，本地+生产双取证 PASS。修本地取证环境：`/api/auth/me` 的 `get_current_user` 要求 `status='approved'`（active 账号登录后 /finance 被重定向）+ 种子数据日期列含非法 `'2026-09'` 致 summary 500 | 待提交 |
| 2026-09-14 | **修复账本导入 xlsx 识别失败（SW `xuanhuang-v80`，User「识别失败了，看下」）**：用户传 xlsx 报「请求失败」+「识别失败」双层错。日志实为 `POST /api/finance/import/analyze-file **422**`（非500）；422 拦截器兜底「请求失败」+ catch「识别失败」同现。根因：`analyzeImportFile` 用共享 `api` 实例（默认 `Content-Type:application/json`）直接 post FormData，被当 JSON 发，后端取不到 `file`→422。修复：`finance.js` 改**原生 fetch**（不设 Content-Type，浏览器自动填 multipart boundary），非2xx 透出 `detail`；`onImportFile` catch 展示真实原因。后端无改动，仅前端 v80 175 文件 home=200；核验 finance chunk 含 `analyzeImportFile`、sw v80 | 本轮 |
| 2026-09-14 | **账本导入支持 Excel 表格 + 任意表头由 AI 识别（SW `xuanhuang-v79`，User「导入要支持表格格式」「表头不一定符合要求，需 AI 分析后展示」）**：新增后端 `POST /finance/import/analyze-file`（multipart，xlsx/xlsm/csv/txt），openpyxl 读 xlsx 转文本后走共享核心 `_analyze_table_text`（AI `finance_csv_import` 归一任意表头→preview 不落库，未配置回退本地）；旧 `.xls` 明确提示另存 `.xlsx`。前端 `analyzeImportFile`(FormData) + 导入按钮 accept 加 xlsx/xlsm/xls，`onImportFile` 按扩展名分发。测试 +3 例全过、全量 pytest 过、前端 137 过(2 偶发超时与本次无关单跑 12/12 过)。部署：后端+openpyxl 安装→pm2 health=200、ENV=production 保留；前端 v79 175 文件 home=200。核验 bundle 含标记、analyze-file 401（已注册）。 | 本轮 |
| 2026-09-14 | **工具页底部操作栏固定+滚动浮现 & PDF输入框间距 & 账本导入AI识别预览（SW `xuanhuang-v78`，User「1-4 工具横栏/间距/同类排查 5.账本导入AI识别精简后上屏」）**：PDF `PdfToolView` 底部 `.pt-actionbar`、像素压缩 `CompressToolView` `.ct-actionbar` 均改为 `position:sticky;bottom:0` 固定滚动容器底 + 监听 `.inner-main` `scrollTop>6` 控制 `.op-hidden`（下滑才浮现、回顶收起，.3s 过渡）；PDF Tab C 两输入框外包 `.c-inputs{gap:14px}` 拉开距离；排查其它工具页（证件照无底部操作栏、其余仅供选择的工具无此横栏）均无需改。**账本导入 AI 识别**：后端 `finance.py` 抽 `_local_parse_csv/_normalize_rows/_insert_transactions`，新增 `POST /finance/import/analyze`（用已配置 AI Provider 能力 `finance_csv_import` 归一杂 CSV 为结构化行返回预览不落库，未配置/异常回退本地解析并标 `is_fake`）+ `POST /finance/import/confirm`（确认后落库）；前端 `onImportFile` 改先 analyze，内联面板（非弹窗）表格预览识别结果 + 确认/取消。测试：新增 `tests/test_finance_import.py` 4 例全过，全量 pytest 254 过、前端 vitest 过。部署：前端 dist2 核验标记（`智能导入预览/fin-import-panel/op-hidden`/SW v78）→替换 dist→175 文件 home=200；后端 finance/ai_providers 同步→pm2 health=200、ENV=production 保留。资产/路由核验：FinanceView bundle 含标记、analyze/confirm 未鉴权 401（已注册） | 本轮 |
| 2026-09-14 | **资讯列表卡片化重设计——成块分层、告别拥挤（SW `xuanhuang-v76`，User「资讯依然拥挤/没层次」）**：生产取证确认 v75 样式已生效属设计质量问题，遂结构性重设计 `FeedsView.vue`：每张卡改**独立玻璃块**（`bg var(--lj-glass)`+边框+圆角16+inset高光）、内边距 `16×14→18×16`、gap `12→14px`；标题恒 **16px/700**（不再随已读降字重）、摘要弱化 13px/灰/两行截断——标题/摘要主次分明；来源胶囊 11px/600、未读赭金/选中朱砂竖条、缩略图 `84×62→96×70`；阅读器 AI 摘要加赭金竖条 + 区块间距 16→18px。仅前端 175 文件 home=200。生产取证 PASS：卡 `bg rgba(22,30,39,.62)` 玻璃块/圆角16/内边距18×16/间距14/标题16px 700/摘要13px 弱化，成块不挤、主次分明（截图 `trae/screenshots/feeds-*.png`） | 833c6be |
| 2026-09-14 | **音乐列表滚动全量曲目 + BGM 任意首次点击恢复 + 资讯/阅读器层级优化（SW `xuanhuang-v75`，User「音乐列表第四个没包含/样式不统一｜BGM 不自动播放｜资讯仍挤需层次感」）**：音乐 `.music-items` 加 `max-height:460px;overflow-y:auto` 细滚动条（4 首全显、样式 100% 统一、第4首「逍遥仙」正常）；BGM `player.js` 恢复句柄 once→**持久监听**（遇拦不消耗，直到出声才离散）+`bgmLibrary.initBgm` 先用默认曲尽早发播再切偏好，保证任意首次点击即自动拉起；资讯中栏列表条目间距 12px/内边距 16/标题 15px/来源胶囊/未读金色竖条/缩略图 84×62；阅读器标题 21px(700)+页头分隔线+头图 340+摘要竖线 3px+正文 15px/行高1.95。vitest 139 过，仅前端部署 175 文件 home=200。生产 Puppeteer 取证 PASS：BGM 进站即播(play resolve 非静音 volume0.3，站点用 `new Audio()` 非 `<audio>`)；音乐 4 首全显无溢出(scrollHeight 356<maxHeight 460)、样式统一；资讯 20 条间距 88px、胶囊/未读强调、标题 21px>正文逐一确认。验证环境浏览器允许自动播放故"进站即播"，未复现被拦路径；拦截策略下由持久恢复句柄兜底 | 64500f7 |
| 2026-09-14 | **资讯页排版优化 + 云端图片缩略图/头图（SW `xuanhuang-v74`，User「资讯字贴一起需优化 / 图片无法展示直接调用云端」）**：`FeedsView.vue` 中栏列表条目间距 `gap 4→10px`、padding `14×12`、行内 gap `6px`、标题/摘要放大——不再拥挤；后端 `FeedArticle` 新增 `image` 列（迁移 `c3d4e5f6a7b8` 已应用），`_fetch.py` `_extract_image` 从 RSS `media_content/media_thumbnail/enclosure/image/首<img>` 提首图，序列化对存量走 `content_html` 首 `<img>` 兜底；前端列表 80×60 缩略图 + 阅读器 `.fd-hero` 头图，`referrerpolicy=no-referrer loading=lazy` 热链云端原图，失败 `onImgError` 隐藏。pytest 254+`_extract_image`8 用例、vitest 139 全过；后端全量同步（pm2 health=200）+ 前端 175 文件 home=200。生产浏览器取证 PASS：列表分区清晰间距舒朗、前 5 篇 4 篇缩略图（热链 img.ithome.com）、阅读器头图正常无裂图 | 本轮 |
| 2026-09-14 | **功能全面补全（SW `xuanhuang-v72`，User「全部做，AI复用对话同一套」，见 `FEATURE_GAP_20260914.md`）**：`/datahub` 个人数据总览看板（聚合+笔记MD/记账CSV导出+自绘SVG图表）、记账月度报表/分类统计/CSV导入导出、RAG 知识问答（复用 `user_ai_provider` 同一套对话 Provider + `build_knowledge_context` 注入）、`/diary` 闪念速记+每日日记时间线（`quick.py`+`QuickView.vue`）、笔记「生成任务草稿」转任务、股票目标价预警+持仓快照趋势、自动备份(`backup_db.py`)+健康监控(`health_check.py`)、nginx manifest MIME。后端全量同步 `deploy_backend_full.py`（补 `werkzeug` 声明依赖，alembic upgrade 补齐 quick_notes/portfolio_snapshots/token_blocklist/FTS5，pm2 重启 health=200）；前端全新 dist2 核验→SW v72→175 文件 home=200。pytest 全量 254 passed。生产 `/datahub`、`/diary` 页 200，新 API 401（已注册），健康检查全 ok | 本轮 |
| 2026-09-08 | **昼夜自适应主题 + 顶栏/玉简精修 + 全站日间审计（SW `xuanhuang-v65/v66`，见 `TIME_THEME_20260908.md`）**：`main.js` 按本地时间设 `html[data-theme]`（6–18 白天，其余夜晚）；`variables.css` 新增日间覆盖层——背景宣纸亮灰、文字深墨阶梯、强调色加深亮底可读、玻璃卡模糊同步亮化；顶栏玻璃分层+朱砂 AI 胶囊+搜索、玉简 3s 轮播+篆符对比度+朱砂环+标签托片+激活长胶囊点；第三轮全站硬编码深底审计（~30 处组件写死深底→日间浅玻璃：笔记选择/灵根测验/K线浮窗/旅行图卡/岛屿页脚/资源预览/日志记账股票证件照输入框/登录卡+输入框/笔记编辑器/工具预览）。功能黑底（视频/照片遮罩）保留。生产 SW v66，169 文件，首页 200；浏览器实测 `data-theme=day` + `.login-card`/输入框 `rgba(255,255,255,.7)` | 本轮 |
| 2026-09-07 | **旅行足迹地图 · 视觉与交互升级（SW `xuanhuang-v53`，见 `TRAVEL_MAP_REDESIGN_20260907.md`）**：夜色雨青玻璃重构 `TravelMap.vue`——省界玻璃青渐变、航线三次贝塞尔+辉光+流动虚线、打点按次数分级+青玉渐变、南海诸岛玻璃 mini-map、两级 hover 浮卡；滚轮缩放(1–14×)+拖拽平移+双击复位、省份下钻（dim 变暗+时间线过滤+清除 chip）、fly-to 聚焦行程、打点中心 75% 内激活行程外环带 isPointInFill 穿透省份下钻。生产取证 PASS：京沪渐变+贝塞尔+mini-map、wheel 缩放生效、点打点激活+fly-to、下钻 chip+cards 过滤+清除恢复、两级浮卡；测试数据已清理库归零 | 待提交 |
| 2026-09-07 | **旅游足迹模块上线（SW `xuanhuang-v52`，见 `TRAVELS_PLAN_20260907.md`，User「标记旅行过的地方」）**：纯前端 SVG 中国地图（离线 GeoJSON `frontend/public/geo/china.json` 省界+九段线 + `geo/cities.json` 34 省 230+ 城市坐标，零第三方地图 API）。后端 `Travel/TravelCity` 模型 + Alembic 迁移 `d1e2f3a4b5c6`（生产 upgrade head 建两表）；`/api/travels`（CRUD + `/stats` + `/upload` 图片≤10MB/视频≤200MB）；公开查看+登录编辑。前端 `TravelMap`（已访省份金色高亮+城市按 seq 打点连线）、`TravelTimeline`、`TravelCityPicker`（省→市两级）、`TravelEditor`（内联录入含 Markdown 正文/星级/相册/视频）、`TravelDetail`（`utils/markdown.js` 安全渲染）；`/travels` 路由（地理数据放 `/geo/` 避 `dist/travels/` 遮蔽）+ 工作台玉简入口。生产浏览器取证 PASS：云南省高亮+丽江打点、星级 ★★★★ 真星星（修 `dist` 陈旧缓存致字面量）、登录新建广东省/广州点亮+Markdown 渲染。验收后已清理测试数据，库归零待录入 | 待提交 |
| 2026-09-06 | **股票查看模块 /stocks 上线（SW `xuanhuang-v48`，见 `STOCKS_PLAN_20260906.md`）**：`StockWatchlist` 模型 + Alembic 迁移 `e5f6a7b8c9d0`；`stock_fetcher`（东财实时报价 / 日 K 三源回退[东财→腾讯→新浪] / 联想搜索，TTL 缓存）；`/api/stocks` CRUD+quote+kline+search+summary+dashboard；`StocksView`(KPI+加自选内联+实时表格)、`StockDetailView`(自绘 SVG 蜡烛图 `KlineChart`) 替换占位页；工作台行情卡接 dashboard。生产实测修复：`f58` 名称字段误用 `_f2`、搜索 `MarketType` 字符串映射、`push2his` WAF 拒连→K 线三源回退 | 262e60f |
| 2026-09-06 | **资讯推送模块上线（SW `xuanhuang-v45`）**：`/feeds` 三栏玻璃布局（源 CRUD / 文章筛选分页 / 阅读器 + AI 摘要 + 收藏为笔记 `to-note`），`FeedSource/FeedArticle` + 迁移 `a1b2c3d4e5f7`；工作台资讯卡接 dashboard。遗留：服务器未配 `AI_*`，摘要为 FakeProvider 占位 | 3f3f138 |
| 2026-09-04 | **工作台极简收敛（SW `xuanhuang-v33`，最新，User「把这三个都干掉」）**：删除工作台数据区三张卡「最近编辑/待整理草稿/标签」（`wb-grid` 与 `WorkbenchView` 脚本树清空，只留玉简+快捷动作+页脚）；前置 v32 已删玉简「内容资产/任务镜台」两张卡 + 「分类入口」整块。工作台现仅含 玉简轮播+快速记录/AI助手+网安页脚。浏览器实测：三卡文本 0 残留、快捷动作+页脚正常 | 本轮 |
| 2026-09-04 | **工作台模块精简：移除「内容资产」+「任务」界面入口，日志升级为操作日志管理页（SW `xuanhuang-v31`，User 遗留「除音视频/文件/工具外的空间需增加管理页」）**：内容资产(`/assets`)与任务(`/tasks`)**仅从界面移除、保留 DB 数据**（顶栏导航只剩笔记、搜索联想去掉资产/任务、工作台删内容资产/任务卡片与今日/逾期任务板块、分类入口去掉网页/图片/PDF/任务、路由删 `/tasks`/`/assets`）；日志页沿用四库**浏览+管理合并一页**（`IslandInnerBase` 重构，浏览=时间线、管理=表格，后端 `/api/logs` 增时间/动作/关键词筛选 + `DELETE /api/logs` 批量删 + `/api/logs/clear` 清空，前端日期范围/动作下拉/搜索/分页/勾选批删/清空，页内中文确认；日志只读不逐条编辑）。生产浏览器清 SW 实测 14 项全 PASS | 本轮 |
| 2026-09-04 | **桌宠本体点击穿透 + 右上角"抓手"拖拽（SW `xuanhuang-v30`，最新，修复"点不进去了"）**：根因——`.whale-frame` 命中区为整 sprite 盒（约 175×215）+ `pointer-events:auto`，鲸鱼拖到玉简/按钮上即整片拦截；`passThroughClick` 透传不可靠。修复：`.whale-frame` 改 `pointer-events:none` **完全点击穿透**；新增右上角 `.whale-handle` 小抓手为唯一命中区（按住拖动、单击开设置面板）；删 `passThroughClick`。清 SW 后实测 PASS：入口跳转恢复 | 本轮 |
| 2026-09-04 | **工具/管理页页脚由"钉底"改"跟随内容"（SW `xuanhuang-v29`）**：短内容页（去水印/PDF/像素压缩/工具详情等）页脚原钉在 100vh 底部，内容少时与内容间大片空隙显得悬浮。`.inner-footer` 移入 `.inner-main` 内随内容流动（`margin-top:16px` 紧贴内容末端）。DOM 取证：工具页页脚与内容间距 16px；音乐管理页 47px 为面板内边距，仍贴面板底边。注：首轮回归"FAIL"系 SW 缓存旧包，旁路后 v29 生效 | 本轮 |
| 2026-09-04 | **修复工作台缩放黑边 + 页脚悬浮（SW `xuanhuang-v28`）**：工作台改全宽浅底（内容与页脚 1248px 居中）消除黑边 + 页面专属浅色页脚；App.vue 全局页脚按路由抑制避免重复/悬空；岛屿页脚先顶到真实底部（后由 v29"跟随内容"取代） | 本轮 |
| 2026-09-04 | **工具统一管理 + 像素压缩 + 全站网安页脚 + 四管理页分页 + 桌面缩放收敛（SW `xuanhuang-v27`）**：① 工具统一管理——`Tool` 增 `kind/is_enabled/sort_order`，内置工具（视频去水印/PDF/像素压缩）置顶且**禁删**、可↑↓排序/上下架，`ToolView` 从 DB 渲染；② 像素压缩 `/tool/compress`——100% 纯前端（智能/自定义/仅缩放，防负优化，ZIP 打包，GIF 转首帧，≤10MB，`revokeObjectURL` 清内存）；③ 全站网安页脚 `SiteFooter`——公安盾牌+皖公网安备(→beian.mps.gov.cn)+皖ICP备(→beian.miit.gov.cn)可跳转，`App.vue`/`IslandInnerBase` 双主题接入；④ 工具/音乐/小说/视频管理页**客户端分页**（默认 10，可切 10/20/50）；⑤ `main.css` 增管理页三档缩放断点，80%~150% 桌面缩放不破版。生产实证 PASS：底部页脚含双备案可跳转、工具管理含三内置且删除禁用、分页控件渲染 | 本轮 |
| 2026-09-04 | **顶栏切换背景音乐双音轨深度修复（SW `xuanhuang-v24`）**：网络层实测复现——切歌瞬间旧/新两条 `/api/music/*/stream` 同时传输。修复 `player.js`：`hardStop()`（`pause+src=''+removeAttribute+load`）硬停、`setBackground` 顶部先停旧歌（早于 await）、`playSeq` 单调序号丢弃过期回调、`armResume()` 单一稳定监听防累积。生产无缓存会话复测 PASS：旧流先 `completed` 结束、新流才开始，无重叠 | 本轮 |
| 2026-09-04 | **播放条收敛 + 去点击音效（SW `xuanhuang-v21`）**：背景音乐（`mode==='bgm'`）不再显示底部播放条（`player.js` 改 `shows=computed(playlist&&curItem)`，仅点播显示，覆盖暂停/取消后恢复背景场景）；去除岛屿卡片悬停/点击音效（移除 `useIslandSound` 整套 + `JadeCarousel` mouseenter/mouseleave 绑定）。生产浏览器实测 6 项全 PASS | 本轮 |
| 2026-09-04 | **内容板块去「岛」重构 + 工具独立页 + 视频解析增强（SW `xuanhuang-v14`）**：五模块去「岛」，`/music /novel /video /tool /tool/:id /tool/watermark /log` 新路由 + 旧 `/island/*` 重定向；每模块浏览+管理合并一页（卡片墙 + 顶部「管理」切表格，全字段+增删改查、音乐默认曲只读）；工具独立页（外部 iframe、去水印 `/tool/watermark` 置顶）；视频解析内联预览 +「下载音频」服务端 ffmpeg 抽音轨。生产装 ffmpeg(dnf/EPEL)，后端修 `starlette.background` 导入。生产浏览器实测 9 项全 PASS | 本轮 |
| 2026-09-03 | **视觉质感升级（玄素琉璃深色材质体系）**：玉简上移防遮挡；桌宠面板 z-index 提至顶栏之上(10001)+最大高度滚动防矮屏遮挡；新增 `--ls-*` 墨青玻璃深色 token，`IslandInnerBase` 背景/header 玉化、5 岛屿内页卡片全部玻璃化，补 `--island-*` 旧色映射防 SVG 落黑；SW 升 `xuanhuang-v11` | 本轮 |
| 2026-09-03 | **工作台重构 + 弃用 /home（"冷峻东方"）**：玉简轮播 `JadeCarousel`（8 张）+ 全局悬浮磨砂顶栏 `GlobalTopBar`（导航/即时搜索联想/音频/用户下拉，移动端折叠）+ 工作台改浅色宣纸留白玉简化；废弃 `/home`、新增 catch-all→`/workbench`；前端追加 `--lj-*` token；SW 升 `xuanhuang-v10`。本地+生产浏览器实测 10 项全 PASS（含 /home 重定向） | 本轮 |
| 2026-09-03 | **鲸鱼娘桌宠上线 + 三轮优化**：登录后全站常驻；单击弹配置面板(固定编排↔随机)、双击散步、拖拽、跑步横穿且朝向修正；Ⅰ缩放75%/池扩；Ⅱ间隔5~7s/淡出淡入；**Ⅲ双层视频真·交叉淡入淡出(无消失空洞)+间隔放宽8~13s**；SW `xuanhuang-v8/v9` | `c5130dc`/`98b1b79` |
| 2026-09-03 | **AI 对话页内联化**：发送免二次确认弹窗、结果在气泡内联应用；SW 升 `xuanhuang-v7` | `bc8faba`/`6ee4888` |
| 2026-09-02 | 修仙风全站统一（工作台 7 子视图 + 5 岛屿琉璃化）；笔记颜色面板 + 吸色；AI 回复修复 | `9db8760` 等 |
| 2026-09-02 | AI Provider 用户级配置；BackButton 收尾 | `dbd24fa`/`9ad3b13` |

> 完整逐日台账见 `WORK_LOG.md -> 一、交付时间线 / 二、关键交付明细`。

## 三、本地开发 / 验证约定

- 本地账号：`admin@yexingchen.cn` / `<见 .secrets/local.env>`。
- 生产账号同邮箱密码（`.secrets/local.env`）。
- 前端 `npm run build` + `npm run test`（vitest，`--no-file-parallelism`）；后端 `.\venv\Scripts\python.exe -m pytest`。
- 部署：`scripts/ai_deploy.py`（传 dist+后端+pm2 restart）；发布前端**务必同步升 `sw.js` 版本**清 SW 缓存。
- 凡「前端显示 / 配置不生效」类问题：**自拉浏览器实测取证截图**，以截图为准（见 `AGENTS.md`）。

## 四、开放 / 遗留事项

> 详细见 `WORK_LOG.md -> 三、开放/遗留事项`。核心：

1. ~~岛屿内部页数据不显示 / `/api/auth/me`、背景音乐流 `ERR_ABORTED`~~ ——**已解决**（本轮浏览器实测内页正常、流播 200）。
2. 视频去水印 `parse-video-py` 各平台解析稳定性待长期观察（独立服务 8070，网络波动/平台变更可能临时不可用）。
3. AI Provider / 笔记等专项单测补齐（部分已补：颜色、AI 解析）。
4. `api_key` 明文存储（用户决定不加密）。
5. `manifest.webmanifest` MIME 待 Nginx 补 `application/manifest+json`。
6. Git 历史旧敏感内容清理（用户「以后做」）；HomeView/LoginView 大拆分（已推迟）。

## 五、回到主线

- 设计/界面新改动：先读仓库内 `docs/DESIGN_INKWASH.md`（唯一生效规范），再落 `docs/ai`（WORK_LOG/本文件）再接续改代码。
- 路线图/PRD：仓库内 `docs/PRD_v2.12.md`；功能总览 `FEATURES.md`；技术决策 `DECISIONS.md`。
